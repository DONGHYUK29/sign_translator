import cv2
import numpy as np

def load_model():
    # 미리 학습된 수어 인식 모델을 로드하는 함수
    # 여기서는 예시로 임의의 모델을 생성
    model = None
    # 실제로는 Keras, TensorFlow 등으로 학습된 모델을 로드해야 함
    return model

def predict_sign_language(model, video_path):
    # 영상 파일을 읽고 수어를 예측하는 함수
    # 이 부분은 실제 모델을 사용해 구현해야 함
    # 여기서는 간단히 임의의 문자열을 반환
    return "Predicted Sign Language"

#고찬영 변경
#고찬영 변경 2